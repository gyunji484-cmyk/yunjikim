<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Winter Snowman</title>
    <style>
        /* 전체 페이지 기본 설정 */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* 배경 설정 - 부드러운 그래디언션 */
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Arial', sans-serif;
        }

        /* 메인 컨테이너 */
        .container {
            text-align: center;
            background: rgba(255, 255, 255, 0.1);
            padding: 60px 40px;
            border-radius: 20px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        /* 제목 스타일 */
        h1 {
            color: white;
            font-size: 48px;
            margin-bottom: 30px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            letter-spacing: 2px;
        }

        /* 눈사람 이미지 */
        .snowman {
            font-size: 150px;
            margin: 30px 0;
            animation: bounce 2s infinite;
        }

        /* 위아래로 튀는 애니메이션 */
        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* 설명 텍스트 */
        .description {
            color: rgba(255, 255, 255, 0.9);
            font-size: 18px;
            line-height: 1.6;
            margin-bottom: 40px;
        }

        /* 버튼 스타일 */
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            padding: 15px 40px;
            font-size: 16px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: transform 0.3s, box-shadow 0.3s;
            font-weight: bold;
            margin: 0 10px;
        }

        /* 버튼 호버 효과 */
        .button:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        /* 버튼 클릭 효과 */
        .button:active {
            transform: translateY(-1px);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1❄️ Winter Magic ❄️</h1>
        
        <div class="snowman">☃️</div>
        
        <p class="description">
            겨울의 신비로운 매력을 느껴보세요.<br>
            눈사람과 함께하는 특별한 순간들이 기다리고 있어요!
        </p>
        
        <button class="button">더 알아보기</button>
        <button class="button">시작하기</button>
    </div>
</body>
</html>